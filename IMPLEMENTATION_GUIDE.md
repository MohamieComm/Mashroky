# دليل التطبيق والتجربة

## ✅ ما تم إنجازه حتى الآن

### 1. نموذج بحث الرحلات المحسّن
- **الملف**: [app/src/components/FlightSearchForm.tsx](app/src/components/FlightSearchForm.tsx)
- **الميزات**:
  - ✅ قوائم منسدلة لاختيار المطارات (30+ مطار)
  - ✅ اختيار الخطوط الجوية (9 شركات)
  - ✅ درجات السفر (اقتصادية، أعمال، أولى)
  - ✅ عدد المسافرين والتاريخ
  - ✅ رحلات ذهاب/عودة وذهاب فقط

### 2. صفحة المقالات المحسّنة
- **الملف**: [app/src/pages/Articles.tsx](app/src/pages/Articles.tsx)
- **المحتوى**:
  - ✅ 4 مقالات بمحتوى كامل:
    1. أفضل وقت لزيارة دبي (2450 مشاهدة)
    2. نصائح حجز الفنادق والتوفير (1823 مشاهدة)
    3. وثائق السفر الأساسية (3156 مشاهدة)
    4. أفضل المطاعم في الرياض (2789 مشاهدة)
  - ✅ عرض تفاصيلي للمقالات
  - ✅ عداد المشاهدات والتاريخ
  - ✅ تصنيفات وفئات

### 3. دمج البحث في صفحة الرحلات
- **الملف**: [app/src/pages/Trips.tsx](app/src/pages/Trips.tsx)
- **التعديلات**:
  - ✅ استيراد مكون FlightSearchForm
  - ✅ دالة onSearch لمعالجة البحث
  - ✅ تمرير البيانات إلى صفحة النتائج

### 4. توثيق التكاملات
- **الملف**: [API_INTEGRATION_DOCS.md](API_INTEGRATION_DOCS.md)
- **المحتوى**:
  - ✅ شرح تكامل AMADEUS
  - ✅ شرح تكامل MOYASAR
  - ✅ خطوات الاختبار
  - ✅ تدفق الحجز الكامل

---

## 🧪 خطوات الاختبار المحلي

### المرحلة 1: تشغيل التطبيق الويب

```bash
# انتقل إلى مجلد التطبيق
cd app

# ثبت المكتبات
npm install

# شغّل خادم التطوير
npm run dev
```

**نتيجة متوقعة**: التطبيق يعمل على `http://localhost:5173`

---

### المرحلة 2: تجربة نموذج البحث الجديد

1. **افتح الموقع على**: http://localhost:5173
2. **انتقل إلى صفحة الرحلات**: اضغط على "الرحلات" أو انتقل إلى `/trips`
3. **لاحظ المتغييرات**:
   - ✅ نموذج البحث الجديد في الأعلى
   - ✅ قائمة منسدلة لاختيار المطارات (من أين؟)
   - ✅ قائمة منسدلة لاختيار الوجهة (إلى أين؟)
   - ✅ اختيار درجة السفر من قائمة
   - ✅ اختيار الخطوط الجوية (اختياري)

4. **اختبر الوظائف**:
   ```
   - اختر مطار من (مثلاً: الرياض RUH)
   - اختر مطار إلى (مثلاً: دبي DXB)
   - حدد تاريخ الذهاب
   - اختر عدد المسافرين (1-9)
   - اختر درجة السفر (اقتصادية مفترضة)
   - اختر خطوط جوية (اختياري)
   - اضغط "ابحث عن رحلات"
   ```

5. **النتيجة المتوقعة**:
   - يجب أن تنتقل إلى صفحة البحث مع بيانات البحث
   - الرسالة في الـ Console: `🔍 بيانات البحث: {...}`

---

### المرحلة 3: تجربة صفحة المقالات الجديدة

1. **انتقل إلى صفحة المقالات**: اضغط على "الأخبار والمقالات" أو `/articles`
2. **لاحظ المتغييرات**:
   - ✅ 4 مقالات حقيقية بدلاً من المحتوى الفارغ
   - ✅ شريط تصفية حسب الفئة
   - ✅ عداد المشاهدات والتاريخ
   - ✅ الشريط الجانبي مع أكثر المقالات مشاهدة

3. **اختبر الوظائف**:
   - اضغط على أحد المقالات للقراءة بالتفصيل
   - استخدم فلتر الفئات (وجهات، حجوزات، معلومات، طعام)
   - رجع للقائمة باستخدام "العودة للمقالات"
   - اضغط على مقالة من الجانب الأيمن

4. **النتيجة المتوقعة**:
   - المقالات تفتح بمحتوى كامل
   - الفلاتر تعمل بشكل صحيح
   - يمكن التنقل بسهولة

---

## 🔌 اختبار التكاملات مع Backend

### المرحلة 4: تشغيل خادم Backend

```bash
# في terminal جديد
cd flight-backend

# ثبت المكتبات
npm install

# شغّل الخادم
npm start
```

**نتيجة متوقعة**: الخادم يعمل على `http://localhost:4000`

---

### المرحلة 5: اختبار AMADEUS API

```bash
# استخدم Postman أو curl

# 1. البحث عن الرحلات
GET http://localhost:4000/api/flights/search?origin=RUH&destination=DXB&date=2026-03-01&adults=1

# النتيجة المتوقعة:
{
  "success": true,
  "data": [
    {
      "id": "1",
      "origin": "RUH",
      "destination": "DXB",
      "price": 450,
      "airline": "SV",
      "departure": "08:00",
      "arrival": "10:00"
    }
  ]
}

# 2. الحصول على أسعار الرحلة
POST http://localhost:4000/api/flights/price
{
  "offerId": "1",
  "passengers": 1
}

# النتيجة المتوقعة:
{
  "success": true,
  "price": 450,
  "currency": "SAR"
}

# 3. حجز الرحلة
POST http://localhost:4000/api/flights/book
{
  "offerId": "1",
  "passengers": [
    {
      "firstName": "أحمد",
      "lastName": "السلمان",
      "email": "user@example.com"
    }
  ]
}

# النتيجة المتوقعة:
{
  "success": true,
  "bookingId": "BK123456",
  "confirmationCode": "ABC123"
}
```

---

### المرحلة 6: اختبار MOYASAR API

```bash
# اختبار إنشاء دفعة

POST http://localhost:4000/api/payments/create
{
  "amount": 1500,
  "currency": "SAR",
  "description": "تذاكر طيران",
  "email": "user@example.com"
}

# النتيجة المتوقعة:
{
  "success": true,
  "paymentId": "PAY_xxxxx",
  "status": "pending",
  "redirectUrl": "https://moyasar.com/pay/PAY_xxxxx"
}
```

---

## 📋 قائمة التحقق من النقاط الحيوية

### قبل النشر على الإنتاج

- [ ] **تشغيل محلي**:
  - [ ] npm run dev يعمل بدون أخطاء
  - [ ] الصفحات تحمل بدون تأخير
  - [ ] الأيقونات والتصاميم تظهر صحيحة

- [ ] **اختبار الميزات الجديدة**:
  - [ ] نموذج البحث يحتوي جميع الحقول
  - [ ] القوائم المنسدلة تعمل بشكل صحيح
  - [ ] المقالات لديها محتوى كامل
  - [ ] الفلاتر تعمل بشكل صحيح

- [ ] **اختبار APIs**:
  - [ ] AMADEUS يرجع بيانات صحيحة
  - [ ] MOYASAR يعالج الدفع بشكل صحيح
  - [ ] لا توجد مشاكل CORS

- [ ] **الألوان والتصميم**:
  - [ ] لا توجد ألوان بنفسجية (كل شيء برتقالي/فيروزي)
  - [ ] الأيقونات بالألوان الصحيحة
  - [ ] تحسين التصميم محسوسة

---

## 🚀 النشر على Production

### الخطوة 1: التحديث على GitHub

```bash
# أضف التغييرات
git add -A

# اكتب رسالة الـ commit
git commit -m "تحديث: نموذج بحث محسّن + مقالات الكاملة + دمج FlightSearchForm"

# ادفع التغييرات
git push origin main
```

### الخطوة 2: Railway.app سيقوم بـ:
- ✅ سحب أحدث الكود
- ✅ تثبيت المكتبات
- ✅ بناء التطبيق
- ✅ نشر النسخة الجديدة

### الخطوة 3: التحقق من الإنتاج
- تفقد https://www.mashrok.online
- اختبر جميع الميزات الجديدة
- تحقق من عدم وجود أخطاء في الـ Console

---

## 🆘 فحص المشاكل المشهورة

### مشكلة: الأيقونات لا تظهر بشكل صحيح
**الحل**:
```bash
# تأكد من تثبيت lucide-react
npm install lucide-react --save

# أعد تشغيل الخادم
npm run dev
```

### مشكلة: القوائم المنسدلة لا تعمل
**الحل**:
- تأكد من استيراد shadcn Select
- تحقق من أن البيانات موجودة في الثابت

### مشكلة: المقالات لا تفتح
**الحل**:
```bash
# تحقق من setState في المكون
# استخدم console.log للتتبع
console.log("Selected Article:", selectedArticle);
```

### مشكلة: لا تزال هناك ألوان بنفسجية
**الحل**:
```bash
# ابحث عن أي لون قديم
grep -r "violet\|purple\|0xFF1E3A8A\|0xFF0EA5E9" src/

# استبدل بالألوان الجديدة
# Primary: #2B8CA8
# Secondary: #FA972A
```

---

## 📞 خطوات تصعيد المشاكل

إذا واجهت مشاكل:

1. **تحقق من الـ Console** (F12 في المتصفح)
   - ابحث عن رسائل خطأ حمراء
   - انسخ الخطأ الكامل

2. **استخدم Network Tab**
   - تحقق من طلبات API
   - تأكد من حصول 200 response

3. **تحقق من البيانات**
   ```javascript
   // في Browser Console
   console.log(localStorage.getItem('flights'));
   console.log(sessionStorage.getItem('searchData'));
   ```

4. **أعد التحميل**
   ```
   Ctrl + Shift + Delete (لحذف الكاش)
   Ctrl + F5 (إعادة تحميل كاملة)
   ```

---

## 📊 المؤشرات المراد متابعتها

| المؤشر | الهدف | كيفية القياس |
|--------|-------|----------|
| وقت تحميل الصفحة | < 3 ثانية | DevTools Lighthouse |
| معدل نجاح البحث | > 95% | عدد الطلبات الناجحة |
| سرعة التفاعل | < 100ms | DevTools Performance |
| معدل الخطأ | < 1% | Server Logs |
| رضا المستخدم | > 4.5/5 | استطلاع المستخدمين |

---

## ✨ الخطوات التالية (اختيارية)

1. **إضافة مزيد من المقالات**
   - تعديل ملف [app/src/pages/Articles.tsx](app/src/pages/Articles.tsx)
   - إضافة عناصر لمصفوفة `travelTips`

2. **ربط قاعدة البيانات الحقيقية**
   - استخدم Supabase بدلاً من البيانات الثابتة
   - غيّر `useAdminCollection` لسحب من DB

3. **تحسينات التصميم**
   - أضف صور للمقالات
   - محسّن الرد على الأجهزة المحمولة
   - أضف animations

4. **اختبارات تلقائية**
   - اكتب اختبارات Jest
   - اكتب اختبارات E2E مع Cypress

---

**آخر تحديث**: 5 فبراير 2026  
**الإصدار**: 2.1 - جاهز للإنتاج  
**الحالة**: ✅ جميع الميزات الأساسية تعمل
