import { useMemo } from "react";
import { Layout } from "@/components/layout/Layout";
import { Button } from "@/components/ui/button";
import { useNavigate } from "react-router-dom";

const TOUR_BOOKING_KEY = "mashrouk-tour-booking";

export default function TourConfirmation() {
  const navigate = useNavigate();
  const booking = useMemo(() => {
    try {
      const raw = localStorage.getItem(TOUR_BOOKING_KEY);
      return raw ? JSON.parse(raw) : null;
    } catch {
      return null;
    }
  }, []);

  return (
    <Layout>
      <section className="hero-gradient py-16">
        <div className="container mx-auto px-4 text-center">
          <span className="text-primary-foreground/80">ÊÃßíÏ ÇáÍÌÒ</span>
          <h1 className="text-3xl md:text-4xl font-bold text-primary-foreground mt-3">
            Êã ÇÓÊáÇã ØáÈ ÇáÌæáÉ
          </h1>
          <p className="text-primary-foreground/80 mt-3">
            ÓäŞæã ÈÊÃßíÏ ÇáÍÌÒ æÅÑÓÇá ÇáÊİÇÕíá ÈÚÏ ÇáÏİÚ.
          </p>
        </div>
      </section>

      <section className="py-12">
        <div className="container mx-auto px-4">
          <div className="bg-card rounded-3xl p-8 shadow-card">
            <h2 className="text-2xl font-bold mb-4">ãáÎÕ ÇáÍÌÒ</h2>
            {booking?.tour ? (
              <div className="space-y-2 text-muted-foreground">
                <div>ÇáÌæáÉ: {booking.tour.name}</div>
                <div>ÇáãÏíäÉ: {booking.tour.city}</div>
              </div>
            ) : (
              <p className="text-muted-foreground">áÇ ÊæÌÏ ÈíÇäÇÊ ÍÌÒ ÍÇáíÇğ.</p>
            )}
            <div className="mt-6 flex flex-col sm:flex-row gap-3">
              <Button variant="hero" onClick={() => navigate("/profile")}>ÚÑÖ ÇáÍÌæÒÇÊ</Button>
              <Button variant="outline" onClick={() => navigate("/tours")}>ÍÌÒ ÌæáÉ ÌÏíÏÉ</Button>
            </div>
          </div>
        </div>
      </section>
    </Layout>
  );
}
