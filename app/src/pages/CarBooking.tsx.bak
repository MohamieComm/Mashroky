import { useMemo, useState } from "react";
import { useLocation, useNavigate, useParams } from "react-router-dom";
import { Layout } from "@/components/layout/Layout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { useCart } from "@/hooks/useCart";

const CAR_SELECTION_KEY = "mashrouk-car-selection";
const CAR_BOOKING_KEY = "mashrouk-car-booking";

type CarResult = {
  id?: string | null;
  name?: string | null;
  category?: string | null;
  transmission?: string | null;
  fuel?: string | null;
  seats?: number | null;
  vendor?: string | null;
  image?: string | null;
  priceTotal?: string | number | null;
  currency?: string | null;
};

type CarSelection = {
  car?: CarResult | null;
};

const parsePrice = (value: unknown) => {
  const numeric = Number(String(value ?? "").replace(/[^\d.]/g, ""));
  return Number.isFinite(numeric) ? numeric : 0;
};

export default function CarBooking() {
  const { addItem } = useCart();
  const navigate = useNavigate();
  const { carId } = useParams();
  const location = useLocation();

  const stored = useMemo<CarSelection>(() => {
    try {
      const raw = localStorage.getItem(CAR_SELECTION_KEY);
      return raw ? (JSON.parse(raw) as CarSelection) : {};
    } catch {
      return {};
    }
  }, []);

  const stateCar = (location.state as CarSelection | null)?.car || null;
  const car = stateCar || stored.car || null;

  const [fullName, setFullName] = useState("");
  const [email, setEmail] = useState("");
  const [phone, setPhone] = useState("");
  const [pickupDate, setPickupDate] = useState("");
  const [dropoffDate, setDropoffDate] = useState("");
  const [notes, setNotes] = useState("");

  if (!car) {
    return (
      <Layout>
        <div className="min-h-[60vh] flex items-center justify-center px-4">
          <div className="bg-card rounded-2xl p-8 shadow-card text-center max-w-lg">
            <h2 className="text-xl font-bold mb-2">·«  ÊÃœ »Ì«‰«  ··”Ì«—…</h2>
            <p className="text-muted-foreground mb-4">Ì—ÃÏ «Œ Ì«— ”Ì«—… √Ê·« „‰ ’›Õ… «·”Ì«—« .</p>
            <Button variant="hero" onClick={() => navigate("/cars")}>«·⁄Êœ… ··”Ì«—« </Button>
          </div>
        </div>
      </Layout>
    );
  }

  const price = parsePrice(car.priceTotal);
  const currency = car.currency || "SAR";

  const handleAddToCart = () => {
    const payload = {
      car,
      traveler: { fullName, email, phone },
      pickupDate,
      dropoffDate,
      notes,
      createdAt: new Date().toISOString(),
    };
    localStorage.setItem(CAR_BOOKING_KEY, JSON.stringify(payload));
    addItem({
      id: `car-${car.id || carId}-${Date.now()}`,
      title: car.name || "ÕÃ“ ”Ì«—…",
      price: price || 0,
      details: `${car.vendor || ""} ï ${currency}`.trim(),
      image: car.image || null,
      type: "car",
    });
    navigate("/cart");
  };

  return (
    <Layout>
      <section className="hero-gradient py-16">
        <div className="container mx-auto px-4 text-center">
          <span className="text-primary-foreground/80">ÕÃ“ «·”Ì«—…</span>
          <h1 className="text-3xl md:text-4xl font-bold text-primary-foreground mt-3">
            √ﬂ„· »Ì«‰«  «·≈ÌÃ«—
          </h1>
          <p className="text-primary-foreground/80 mt-3">{car.name}</p>
        </div>
      </section>

      <section className="py-12">
        <div className="container mx-auto px-4 grid lg:grid-cols-[1.2fr_0.8fr] gap-8">
          <div className="bg-card rounded-3xl p-6 shadow-card space-y-4">
            <h3 className="text-xl font-bold">»Ì«‰«  «·„” √Ã—</h3>
            <Input value={fullName} onChange={(e) => setFullName(e.target.value)} placeholder="«·«”„ «·ﬂ«„·" />
            <Input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="«·»—Ìœ «·≈·ﬂ —Ê‰Ì" />
            <Input value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="—ﬁ„ «·ÃÊ«·" />
            <div className="grid md:grid-cols-2 gap-4">
              <Input type="date" value={pickupDate} onChange={(e) => setPickupDate(e.target.value)} />
              <Input type="date" value={dropoffDate} onChange={(e) => setDropoffDate(e.target.value)} />
            </div>
            <Textarea value={notes} onChange={(e) => setNotes(e.target.value)} placeholder="„·«ÕŸ«  ≈÷«›Ì…" />
          </div>

          <div className="bg-muted rounded-3xl p-6 shadow-card h-fit">
            <h3 className="text-xl font-bold mb-4">„·Œ’ «·≈ÌÃ«—</h3>
            <div className="space-y-2 text-sm text-muted-foreground">
              <div>«·”Ì«—…: {car.name}</div>
              <div>«·„“Êœ: {car.vendor || "ó"}</div>
              <div>«·›∆…: {car.category || "ó"}</div>
            </div>
            <div className="mt-6 text-2xl font-bold text-primary">
              {price ? price.toLocaleString() : "ó"} {currency}
            </div>
            <Button variant="hero" className="w-full mt-6" onClick={handleAddToCart}>
              ≈÷«›… ··”·… Ê«·„ «»⁄… ··œ›⁄
            </Button>
          </div>
        </div>
      </section>
    </Layout>
  );
}
